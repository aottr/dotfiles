# dotfiles

1. Clone repo
```bash
git clone --depth 1 https://github.com/aottr/dotfiles.git ~/.dotfiles
```
2. Enter the dotfiles directory
```bash
cd ~/.dotfiles
```

3. Run stow to install the dotfiles
```bash
stow .
```